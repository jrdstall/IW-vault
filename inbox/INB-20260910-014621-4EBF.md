with all the talk of the lack of power for all the coming data centers, can we talk about GREEN code, storage, data, transfer, compute, memory?  like we used to design software to fit into a few kilobytes of memory and compute (think Apollo) we now waste so much compute doing worthless things like rending transitoins on GUIs.  we also make code super inefficient all over the place, is it time to return back to optimizing code for power?  can co-design principles help? can we use analog computing in places where we waste power with digital?  can we stop letting AI do stuff it's not best to do?
